export enum CompanyStatus {
    Open,
    Closed,
    ComingSoon
}

export enum Role {
    admin = 1,
    user = 2
}

export enum AccountStatus {
    pending = 1,
    approved = 2,
    blocked = 3,
    deleted = 4
}


export enum NotificationType {
    newRegister = 1,
    deleted = 2,
    blocked = 3
}